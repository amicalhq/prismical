/**
 * Public docs deep links surfaced from error states. One module so a docs
 * reshuffle is a one-line change, not a grep across error banners.
 */
export const RECORDING_TROUBLESHOOTING_URL = 'https://prismical.ai/docs';
