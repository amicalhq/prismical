{
  "name": "@prismical/silence",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Framework-free silence detection + auto-pause state machine shared by the web renderer and the desktop MAIN process. Zero runtime dependencies.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src/ --max-warnings 0",
    "type:check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "@prismical/eslint-config": "workspace:*",
    "@prismical/typescript-config": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
