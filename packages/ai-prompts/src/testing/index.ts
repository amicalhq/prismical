/**
 * Fixtures for exercising the audio paths. A separate entry point
 * (`@prismical/ai-prompts/testing`) keeps them out of production barrels while
 * sharing one `buildTestWav` implementation across tests.
 */
export * from './wav.js';
